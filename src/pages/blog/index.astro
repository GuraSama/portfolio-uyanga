---
import { getCollection } from 'astro:content';

import Layout from '../../layouts/Layout.astro';
import PostCard from '../../components/PostCard.astro';

const posts = (await getCollection('blog'))
	.filter((post) => !post.data.draft)
	.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<Layout
	title="Blog | Uyanga"
	description="Read published articles managed through the Decap CMS writing workflow."
>
	<section class="page-hero panel">
		<div class="eyebrow">Blog Archive</div>
		<h1>Published writing, notes, and case studies.</h1>
		<p>
			Every post on this page comes from the same CMS workflow used by the writer dashboard, so
			adding content stays simple.
		</p>
	</section>

	{
		posts.length > 0 ? (
			<div class="post-grid">
				{posts.map((post) => (
					<PostCard post={post} />
				))}
			</div>
		) : (
			<section class="panel empty-state">
				<h2>No posts published yet</h2>
				<p>Open the dashboard, create a post, and it will appear here after publishing.</p>
			</section>
		)
	}
</Layout>
